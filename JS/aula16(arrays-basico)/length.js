let alunos = ['Luiz', 'Maria', 'João'];
console.log(alunos.length); // pega a quantidade dos elementos

alunos = ['Daniel', 'João', 'Eduardo'];
alunos[alunos.length] = 'Luiza'; // serve tambem para adicionar elementos no final
alunos[alunos.length] = 'Luana';

console.log(alunos);